version: "3"
services:
  schnittpunkt-frontend:
    build: ./frontend
    volumes:
      - ./frontend:/usr/local/apache2/htdocs
    ports:
      - 7891:80
  schnittpunkt-api:
    build: ./api
    volumes:
      - ./api:/usr/src/app
    ports:
      - 7892:2342
